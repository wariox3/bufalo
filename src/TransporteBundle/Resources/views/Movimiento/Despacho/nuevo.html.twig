{% extends '::base.html.twig' %}
{% block content %}
    <section class="wrapper">
        <h3><i class="fa fa-angle-right"></i> Movimiento: Nuevo -> despacho</h3>
        <div class="row mt">
            <div class="col-lg-12">
                <div class="form-panel">                    
                    <h4 class="mb"><i class="fa fa-angle-right"></i> Nuevo: Despacho</h4>
                    {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
                        <div class="form-group">
                            <label class="col-sm-2 col-sm-2 control-label">Fecha:</label>
                            <div class="col-sm-10">
                                {{ form_widget(form.fecha, { 'attr': {'class': 'form-control'} }) }}
                            </div>
                        </div>                         
                        <div class="modal-footer">
                            <a class="btn btn-default btn-sm" href="{{ path('tte_movimiento_despacho') }}">Cancelar</a>        
                            {{ form_widget(form.guardar, { 'attr': {'class': 'btn btn-primary btn-sm'} }) }}
                            {{ form_widget(form.guardarnuevo, { 'attr': {'class': 'btn btn-primary btn-sm'} }) }}
                        </div>                            
                    {{ form_end(form) }}
                </div>
            </div><!-- col-lg-12-->      	
        </div><!-- /row -->

    </section><! --/wrapper -->
{% endblock %}
